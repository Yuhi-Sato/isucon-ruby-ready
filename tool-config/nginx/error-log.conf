# nginx.conf の http {} ブロック（または対象 server {} ブロック）内に貼り付ける。
# デフォルトのレベル（error）より狭いレベル（crit/alert等）に変更されている場合のみ、
# 元に戻す目的で使う。デフォルトのままなら変更不要（error-log-setup.md参照）。

error_log /var/log/nginx/error.log warn;
