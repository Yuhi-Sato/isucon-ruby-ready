# 用語集

このリポジトリ（ISUCON当日用ツール一式）で使う用語の定義。実装詳細は各スクリプト・[README.md](README.md)を参照。

## セットアップ

問題によらず同一の手順で行う、サーバーのインフラ準備。ツール導入（alp/notify_slack/pt-query-digest等）・git配線（チームリポジトリの取得と配布コードのpush）・サーバー実設定（DB/nginx）の取得までを指す。`setup.sh`（ローカル）と`remote-setup.sh`／`server-setup.sh`（サーバー）の責務。

チームリポジトリはテンプレートから人が事前に作る（`gh repo create --template`）。作成はセットアップの範囲に含めない。

対比: [[初動調査]]と[[計測セットアップ]]は問題ごとに異なる調査・適応作業であり、セットアップの範囲に含めない。

## 初動調査

競技開始直後にエージェント（`isucon-initial-recon`スキル）が行う、問題の把握。レギュレーション読解、アプリのルート一覧と初期化エンドポイントの読解、DBスキーマ・インデックスの調査、実サービス名の特定までを指す。結果は`docs/recon.md`にまとめる。

対比: 調査であって設定変更ではない。特定した値を実際に反映するのは[[計測セットアップ]]の責務。

## 計測セットアップ

初回ベンチマークの前にエージェント（`isucon-measurement-setup`スキル）が行う、計測基盤の問題適応。`scripts/vars.sh`の変数（`APP_DIR`/`SERVICE_NAME`/`DB_SERVICE_NAME`）の修正、`tool-config/alp/config.yml`のmatching_groups調整、nginxのltsvログフォーマット、MySQLの`performance_schema`、ユーザー行動履歴ロガーの導入を含む。

未完了のままベンチを実行すると計測データが使いものにならず、ベンチをやり直すことになる（ベンチデータは後から取り返せない）。

対比: 問題によらないインフラ準備である[[セットアップ]]とは別物。こちらは問題ごとの適応であり、`setup.sh`は行わない。

## 役割（s1/s2/s3）

競技サーバー1台ごとに割り当てるラベル。`~/.ssh/config`のHostエイリアス名として使う（`setup.sh`が`servers.conf`から生成する）。

- **s1**: メインサーバー。配布アプリコードをチームリポジトリへ初回pushする
- **s2 / s3**: 2台目以降。s1がpushしたmainを取得してセットアップに参加する

`setup.sh`は役割を第1引数で受け取る（`./setup.sh`は`s1`、`./setup.sh s2`は`s2`）。s2/s3はs1のpushに依存するため、必ずs1を先に完了させる。

## ユーザー行動履歴

ベンチマーカーが演じる仮想ユーザー1人ひとりのリクエスト時系列。「ユーザー」はアプリのセッションが指す**ユーザーID**で識別する（remote_addrやUser-Agentではない。ベンチマーカーは少数のIPから多数のユーザーを演じるため）。

実体はnginxアクセスログ（LTSV）に`userid`フィールドを加えたもの。ユーザーIDはRackミドルウェアがレスポンスヘッダー経由でnginxに渡し、nginxがログに記録する（クライアントには返さない）。

対比: alpの集計はエンドポイント軸の横断集計であり、ユーザー軸の時系列（行動フロー）は扱えない。行動履歴の分析はそのギャップを埋める。

## deploy と bench-prep

- **`make deploy`**: 軽量デプロイ。ログを消さず、DB/nginxを再起動しない。サーバー上で手動実行するか、ローカルから`make remote-deploy-s1`で叩く
- **`make bench-prep`**: ベンチマーク実行直前に人間が手動で叩く。ログ削除・設定反映・DB/nginx含む全再起動を伴う

両者を混同して`make bench-prep`を計測中に実行すると、他メンバーの計測ログが消え、DB/nginxが瞬断する。
